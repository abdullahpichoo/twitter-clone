<%= turbo_stream.replace "follow-unfollow-btn" do %>
  <%= render partial: 'users/follow_unfollow', locals: {user: @user , current_user: current_user} %>
<% end %>
<%= turbo_stream.replace "follow-count" do %>
  <%= render partial: 'users/follow_count', locals: {user: User.find(params[:user_id]) } %>
<% end %>
<%= turbo_stream.replace "follow-unfollow-btn-user_#{@user.id}" do %>
  <%= render partial: 'users/follow_unfollow', locals: {user: @user,  current_user: current_user } %>
<% end %>
